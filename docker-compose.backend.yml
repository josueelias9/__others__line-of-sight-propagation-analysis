services:
    
  backend:
    volumes:
      # Mount the root folder that contains .git
      - .:/workspace
    command: uvicorn api:app --host 0.0.0.0 --port 8000 --reload


  frontend:
    profiles:
      - avoid